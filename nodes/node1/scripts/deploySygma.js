(async () => {
  const { ethers } = require("ethers");
  const {
    Bridge__factory,
    BasicFeeHandler__factory,
    ERC20Handler__factory,
  } = require("@chainsafe/chainbridge-contracts");
})();
